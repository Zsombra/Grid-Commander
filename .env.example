# BattleGrid MCP — copy to .env and fill in. .env is git-ignored.
# Get a key from battlegrid.trade. This key moves REAL money (mcp:wager scope) —
# treat it like a production credential and rotate if it is ever exposed.
BG_API_KEY=bg_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
BG_MCP_ENDPOINT=https://mcp.battlegrid.trade/mcp
