# Active changes

One folder per proposed change. Create with `/propose`.

Completed changes move to `archive/` with a date prefix, deltas merged into
`openspec/specs/`.

Lifecycle: `.claude/references/change-lifecycle.md`
