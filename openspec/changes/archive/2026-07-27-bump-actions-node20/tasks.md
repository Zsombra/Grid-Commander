# Tasks

- [x] 1 Bump `actions/checkout` to v5 and `actions/setup-python` to v6 in both
      jobs
- [x] 2 Confirm CI resolves both versions and stays green — an unresolvable
      action fails the job immediately, which is the verification
- [x] 3 Confirm the Node 20 deprecation warning is gone from the run log
- [x] 4 Close `bump-actions-node20`
