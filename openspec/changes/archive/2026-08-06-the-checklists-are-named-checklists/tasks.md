# Tasks

- [x] `git mv docs/specs docs/checklists` (history follows)
- [x] Rewrite every live reference — skills, commands, references, config,
      CLAUDE.md, README.md, HANDOFF.md, docs, backlog items
- [x] Retitle the three "two things named spec" headings; keep their tables
- [x] Leave the archive, JOURNAL.md and CHANGELOG.md as dated records, and say
      so in the proposal
- [x] Grep proves no live `docs/specs` reference survives (the three that
      remain all deliberately name the old path)
- [x] `./scripts/check.sh` — harness tests + spec validation
- [x] `openspec.py board` and `validate --all` still resolve
- [x] Full local CI
