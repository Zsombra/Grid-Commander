# Tasks

## 1. The config owns the pacing

- [x] 1.1 `vitest.live.config.ts` — tests/live only, file parallelism off
- [x] 1.2 `test:live` script; HANDOFF's sweep commands name it
- [x] 1.3 Close `live-probes-run-concurrently-by-default`; gates green
