# Tasks

- [x] 1.1 `StrategySection` carries title/timeframe/columns; mapper reads
      them; `previewReport` sends them back for custom sections.
- [x] 1.2 Tests: mapper keeps the definition, preview passes it through,
      platform sections stay key-only; live probe green; gates green.
