# Tasks

## 1. Split
- [x] 1.1 `app` job `runs-on` reads `CI_APP_RUNNER` (falls back to
      `ubuntu-latest`), independent of `CI_RUNNER`; YAML still parses.
- [x] 1.2 Handout updated: Docker-less path (6/7 green), when to set which
      variable, `app` options.
- [x] 1.3 `validate --all` zero errors.
