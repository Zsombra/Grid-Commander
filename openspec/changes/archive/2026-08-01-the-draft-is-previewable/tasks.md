# Tasks

- [x] 1.1 Port reads (`previewReport`, `deriveRuleView`) + adapter mapping
      the live shapes; refusals surface as results in the platform's words.
- [x] 1.2 `PreviewCompositionQuery` (fresh detail read → both tools in
      parallel → one result); composition wiring.
- [x] 1.3 `/strategies/[id]/preview` — coin-selection form (ranked N /
      explicit tickers), rendered sections, token estimate, budget gauges,
      membership summary; linked from the strategy page.
- [x] 1.4 Tests: mapper, query states, rendering per branch; key-gated live
      probe; gates green.
