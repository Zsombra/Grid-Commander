# Tasks

- [x] 1.1 `SignalVerdict` on the port; extend `EntryDecision` with the
      checklist, sizing, horizon, volatility, timings and order ids.
- [x] 1.2 `mapEntryDecision` maps the checklist whole, keeping the
      platform's verdict vocabulary and each written interpretation.
- [x] 1.3 Render the evidence under each decision on
      `/agents/[id]/pipeline`, with the verdict spread visible at a glance.
- [x] 1.4 `MoneyLimits`: the approval-required option states that
      Grid-Commander cannot yet answer what the agent proposes.
- [x] 1.5 Tests: mapper over the live shapes (including a decision with no
      checklist), rendering per verdict, the mode copy; live probe
      extended; all nine gates green.
