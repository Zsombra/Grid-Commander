# Tasks

- [x] Executor skill: the unification-ships-its-guard rule in Step 5
      (Enforce Implementation Rules)
- [x] UI checklist: the same rule as Tailwind row 6 with its why; version
      1.1.0, dated 2026-08-07; review output template count follows (X/5 →
      X/6)
- [x] change-lifecycle.md: integrator paragraph — after a parallel round
      merges, re-run round-introduced guards against the merged tree
- [x] Backlog item `a-sweep-cannot-see-files-born-in-the-same-round` set to
      done, linked here, with a where-the-rule-lives note
- [x] `python3 .claude/tools/openspec.py validate a-unification-ships-its-guard`
      — zero errors
- [x] `./scripts/check.sh` — harness tests + spec validation
