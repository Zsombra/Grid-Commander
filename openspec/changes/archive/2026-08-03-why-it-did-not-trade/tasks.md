# Tasks

- [x] 1.1 Port reads + adapter for gate blocks, signal logs and entry
      decisions, each mapped whole with the platform's reason codes and
      quantified details kept.
- [x] 1.2 `ReadPipelineQuery` — three stages in parallel, each
      independently empty/unreadable; composition wiring.
- [x] 1.3 `/agents/[id]/pipeline` + link from the agent page.
- [x] 1.4 Tests: mappers over the live shapes, per-stage independence,
      rendering per branch; live probe; gates green.
