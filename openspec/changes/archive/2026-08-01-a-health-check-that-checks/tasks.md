# Tasks

- [x] 1.1 `checkHealth()` in the composition (captures the pool at build,
      `select 1`, resolves nothing else) + `app/api/health/route.ts`.
- [x] 1.2 Route tests (ok / unavailable / no leak); serving gate probes it.
