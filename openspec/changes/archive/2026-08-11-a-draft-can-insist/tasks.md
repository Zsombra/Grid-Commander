# Tasks — a draft can insist

- [x] 1. Composer: the "Holding" select, default optional; the seeded note
      names the flag and the ignored control.
- [x] 2. Form: fresh compose reads `holding`; must-hold → true, anything
      else → false; comment updated, filed-item note removed.
- [x] 3. Tests: parsing (explicit, absent, unoffered value); rendering
      (control offered, seeded note).
- [x] 4. Suite green; #88 item + issue closed; validate + archive.
