# Tasks

- [x] 1.1 Port read + adapter for `list_trade_outcomes`, mapped whole
      (fees, slippage, conviction, close reason, duration, links).
- [x] 1.2 `ReadTradingRecordQuery` — page plus a derived summary that says
      it is derived; empty and unreadable stay distinct.
- [x] 1.3 `/agents/[id]/trades` + link from the agent page; paging.
- [x] 1.4 Tests: mapper over the live shape, summary arithmetic, query
      states, rendering per branch; live probe; gates green.
