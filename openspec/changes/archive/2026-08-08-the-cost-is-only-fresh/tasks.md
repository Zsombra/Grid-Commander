# Tasks

## 1. The probe tells the truth about a transient field

- [x] 1.1 Replace the unconditional not-null assertion: cost present → shape
      asserted; cost absent → the mapped evaluation states it as unreported,
      and the probe prints which case ran
- [x] 1.2 Close `an-owned-evaluations-cost-reads-null` with the evidence
- [x] 1.3 Gates: typecheck, lint, vitest (probe skips keyless); keyed rerun
