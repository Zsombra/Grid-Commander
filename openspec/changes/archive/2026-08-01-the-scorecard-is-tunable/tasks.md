# Tasks

- [x] 1.1 `confirmationTarget.strategyRule` (strategy, revision, signal,
      values digest) + port write + adapter with the `{request:…}` envelope.
- [x] 1.2 `DescribeRetuneQuery` (fresh read, membership, no-op, consequence
      with blast radius + the platform's propagation wording, token) and
      `RetuneRuleCommand` (recomputed target, refusals as results).
- [x] 1.3 `/strategies/[id]/rules/[signalId]` two-step page; declared
      params prefilled; `?problem=` channel; rule rows on the strategy page
      link in; composition wiring.
- [x] 1.4 Guards: payload-conformance case; reachability pins; edit-binding
      and concurrency hold with no exemptions added.
- [x] 1.5 Tests: describe/perform branches incl. tamper refusal; rendering
      per branch; gates green.
- [x] 1.6 Live walk on a zero-bound fork via the slot shuffle (platform
      permitting — 504 spells all day; record honestly either way).
