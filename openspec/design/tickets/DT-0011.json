{
  "id": "DT-0011",
  "surface": "agent-roster",
  "targets": [
    "agent-roster"
  ],
  "type": "restyle",
  "priority": "p3",
  "status": "implemented",
  "behavior_impact": "none",
  "spec_change": "",
  "intent": "The roster's message blocks stop wearing the anonymous border: an unreadable roster speaks in danger, the two advisory banners speak in notice, and empty stays prose. The first ruling on the unreadable role, stated here and reused by DT-0013.",
  "rationale": "DT-0004 built the hierarchy — a bounced attempt speaks in danger, advisory speaks in notice, consequence outranks everything — but no ticket has ruled on a page whose whole read failed: DT-0009 left 'unreadable' explicitly unchanged. The ruling: unreadable wears danger. The code already treats it as a failure (role=alert, 'could not be loaded'), and 'advisory is not failure' (system.json) cuts both ways — dressing a failed read as a notice teaches users that notice sometimes means broken, which spends the calm that makes notices readable. The reassurance sentence (why-not-loaded) stays inside the danger block: the block reports the failure honestly; the text is what keeps it from reading as loss. The two role=status banners — create-at-capacity and radar-unreadable — are advisory (the page works; nothing the user did failed) and take notice; the radar one is currently a bare paragraph with no box at all, invisible next to the bordered rows it governs. Empty is an answer, not an event: plain prose, unchanged. Row cards already wear the swept token border and stay put.",
  "design": {
    "layout": "Unchanged — create affordance, then the list; unreadable replaces the page body.",
    "tokens": {
      "source": "openspec/design/system.json",
      "danger_bg": "color.danger.subtle",
      "danger_border": "color.danger.default",
      "notice_bg": "color.notice.subtle",
      "notice_border": "color.notice.border",
      "block_radius": "radius.2",
      "block_padding": "space.4",
      "banner_padding": "space.3",
      "text": "color.text.primary",
      "demoted": "color.text.secondary",
      "heading_weight": "type.weight.semibold"
    },
    "states": {
      "default": {
        "content": "Unchanged — the create line, then the bordered rows at their existing rhythm."
      },
      "empty": {
        "content": "Unchanged — plain prose. An account with no agents is an answer, not an event; boxing it would rank it against the notices."
      },
      "unreadable": {
        "content": "The block takes danger: color.danger.subtle background, 1px color.danger.default border, radius.2, space.4 padding, color.text.primary text. The heading 'Your roster could not be loaded.' takes type.weight.semibold. role=alert stays. The why-not-loaded reassurance and the nothing-can-be-created sentence stay inside the block, unabridged — the tint says something failed; the words say what did not."
      },
      "create-available": {
        "content": "Unchanged — the underlined link and the remaining-slots count as plain prose."
      },
      "create-at-capacity": {
        "content": "The banner takes notice: color.notice.subtle background, 1px color.notice.border border, radius.2, space.3 padding, color.text.primary. role=status stays. Advisory, never danger — a full account is a fact about limits, not a failure."
      },
      "create-unknown": {
        "content": "Renders nothing, unchanged — an affordance whose availability is unknown is not guessed into existence (manifest constraint)."
      },
      "deployment-unavailable": {
        "content": "The radar-unreadable notice gains its box: color.notice.subtle background, 1px color.notice.border border, radius.2, space.3 padding, color.text.primary. role=status stays. Advisory relative to this page — the roster itself loaded — and the manifest's own words call it 'one notice for the whole list'. Never danger: rows below it claim nothing either way, and a danger banner would recolour that silence as breakage."
      },
      "agent-row:active": {
        "content": "Unchanged — the status word in its existing small uppercase; the word is the signal, per 'never colour alone'."
      },
      "agent-row:archived": {
        "content": "Unchanged — archived is recoverable (agent-authoring spec) and the row must not read as lesser; the status word carries it."
      },
      "agent-row:overflow-long-name": {
        "content": "The name link takes break-words so a long display name wraps inside the card instead of pushing the status off the row; the baseline flex keeps flex-wrap so status drops to its own line when crowded. No truncation — the full name is the identity."
      },
      "agent-row:deployment-acting": {
        "content": "Unchanged — plain prose sharing the detail page's words (manifest constraint: the two surfaces must not disagree)."
      },
      "agent-row:deployment-waiting": {
        "content": "Unchanged — 'Not deployed — scanning no market' stays plain prose; absence is stated in words, and the detail page's wording binds."
      }
    },
    "responsive": {
      "mobile": "Blocks and cards full width; the row's action links keep their existing wrapped gap-3 spacing.",
      "desktop": "Unchanged."
    },
    "motion": {
      "none": "Static content; no motion introduced."
    },
    "content": {
      "note": "Zero copy changes."
    }
  },
  "acceptance": [
    "The unreadable branch renders on color.danger.subtle with a 1px color.danger.default border at radius.2, keeps role=alert, and its heading is type.weight.semibold",
    "The why-not-loaded reassurance and the nothing-can-be-created sentence render inside the danger block, verbatim",
    "The create-at-capacity banner renders on color.notice.subtle with a 1px color.notice.border border and keeps role=status",
    "The radar-unreadable notice renders in the same notice treatment as the capacity banner and keeps role=status",
    "The empty state remains unboxed plain prose",
    "A 60-character agent name wraps within its card without pushing the status word out of view",
    "No block on this page other than the unreadable branch uses any color.danger token",
    "Zero copy changes anywhere on the page",
    "No raw color, spacing, or size literal is introduced"
  ],
  "references": [
    {
      "kind": "note",
      "value": "Backlog 157 (the design lane's tail) — first ticket for this surface. The unreadable-wears-danger ruling is restated by DT-0013 for the strategy catalog; DT-0009 had left the role undecided."
    }
  ],
  "created": "2026-08-12",
  "updated": "2026-08-12",
  "github_issue": null
}
