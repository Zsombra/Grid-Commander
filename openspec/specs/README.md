# Source of truth

One folder per capability: `<capability>/spec.md`.

These files describe how the system behaves **today**. They are written by the
archiver when a change is archived — do not hand-edit them during a change.
Write delta specs in `openspec/changes/<change-id>/specs/` instead.

Format: `.claude/references/spec-format.md`
