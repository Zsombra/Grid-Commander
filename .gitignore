# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code per-user state (if it leaks into a project)
.claude/settings.local.json
.claude/projects/
.claude/todos/

# Python bytecode
__pycache__/
*.py[cod]

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Local env
.env
.env.local
.env*.local

# Raw MCP capture dumps — regenerate with tools/capture_mcp_dump.py.
# Not committed: they are a means to the two artifacts in docs/, and a stale
# copy in the tree is a second source of truth nobody refreshes.
build_log/
